/*Son utiles para metodos de click, son concisas
no usa el this se evita creo, son utiles para accines
directas y que no se reutilizen las evisto en 
artos codigos*/

const sumarfuncionflecha = (a,b) => a + b;
let resultado = sumarfuncionflecha(3,5);

alert(resultado);